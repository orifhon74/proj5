func main :: num ()
begin
    var x :: num ;
    var y :: num ;
    var z :: bool;
    x := 1;
    y := 0;
    z := x > 10;
    while (not z) and (y >= 0)
    begin
        y := y + x;
        x := x + 1;
        z := x > 10;
    end
    print x;
    print y;
    return 1 ;
end
